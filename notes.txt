-am reparat functia de comparare din versiunea trecuta
-am adaugat doua functii mostenite de la Manual
-am adaugat exceptii
-am incercat sa schimb si stocul pe Librarie doar ca am avut niste probleme
Am declarat libraria asa:

class Librarie{
private:
    std::string name;
    std::vector < std::pair <Caiet, int> > caiete;
    std::vector < std::pair <Manual, int> > manuale;
}

si nu stiu cum ar trebui sa rescriu functiile de get si set pe care le aveam inainte pentru stoc pentru a putea modifica stocul, 
asa ca mi-au aparut niste probleme la functiile adauga si sterge(+ aici nu-mi mai merge sa sterg de tot elementul din vector, inainte il stergeam cu:
caiete.erase(remove(caiete.begin(), caiete.end(), caiet)); )

Ca exemplu la ce am incercat pentru functia adauga si nu imi modifica stocul:

void Librarie::adauga(Caiet &caiet, int stoc){  ///verifica daca produsul exista deja, iar daca exista ii modifica stocul, daca nu, il adauga la librarie
    int ok = 1;
    for(auto &c : this->caiete)
        if( c.first == caiet)
        {
            c.second = c.second + stoc;
            ok = 0;
            break;
        }
    if(ok == 1) caiete.push_back( std::make_pair (caiet, stoc));
}
